# Repo root — docker compose / scripts; copy to .env (gitignored)

LLM_PROVIDER=ollama
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2
NCBI_EMAIL=your@email.com
QDRANT_URL=http://localhost:6333
EMBEDDING_BACKEND=auto

# API production (Render): services/peggy-api/.env.render.example
